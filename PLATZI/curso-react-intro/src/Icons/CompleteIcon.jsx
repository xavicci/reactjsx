import React from 'react';
import { TodoIcon } from './TodoIcon';

export const CompleteIcon = () => {
    return (
        <TodoIcon
            type="check"
            color="gray"
        />
    );
}